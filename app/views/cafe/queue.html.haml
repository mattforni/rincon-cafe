- if @queue.empty?
  -# TODO make order a link to new
  = 'The queue is empty! Hurry and order!'
- else
  - @queue.each do |order|
    = queued_partial order
  #actions
    #add
      - if current_user.can_order?
        = link_to image_tag('icons/add.png', size: '25x25'), new_order_path
      - else
        = Order::QUEUE_FULL_MESSAGE

